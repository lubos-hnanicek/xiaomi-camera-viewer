#pragma once

// Resource identifiers, shared by app.rc and the code that loads from it.
//
// The application icon takes 1 because Explorer shows whichever icon group has
// the lowest identifier, and a shortcut created against "the first icon" has to
// keep meaning this one however many are added later.
#define IDI_APPICON 1
